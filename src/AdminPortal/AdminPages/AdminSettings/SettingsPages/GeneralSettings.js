import React from 'react';

const GeneralSettings = () => {
  return <h2>General Settings Page</h2>;

  // To add: Default Time Zone,
  // Work Hours
  // 
//   <label>
//   Default Time Zone:
//   <select>
//     {timezones.map((zone) => (
//       <option key={zone} value={zone}>
//         {zone} ({moment.tz(zone).format('z')})
//       </option>
//     ))}
//   </select>
// </label>
};

export default GeneralSettings;